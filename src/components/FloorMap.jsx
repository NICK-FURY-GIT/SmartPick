export default function FloorMap({ division }) {
  return (
    <div className="flex items-center justify-center h-full min-h-[400px] rounded-xl border border-white/5 bg-white/[0.02]">
      <p className="text-white/20 text-sm font-mono">
        FloorMap — {division} (coming soon)
      </p>
    </div>
  )
}
